services:
  pffdtd:
    build:
      context: .
      args:
        PFFDTD_REPO: "https://github.com/mskarha/pffdtd-dev"
        CUDA_ARCH: "sm_89"
    gpus: all
    tty: true
    stdin_open: true
    volumes:
      - pffdtd-work:/work
      - /home/ubuntu/dev/pffdtd-dev:/src/pffdtd
      - /home/ubuntu/pffdtd-outputs:/host-data
volumes:
  pffdtd-work:
